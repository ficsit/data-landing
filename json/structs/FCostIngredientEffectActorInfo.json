{
  "type": "object",
  "properties": {
    "ItemClass": {
      "description": "What class is this cost of",
      "type": "object",
      "properties": {
        "package": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "package"
      ]
    },
    "SpawnDelay": {
      "description": "Delay between this and the previous cost actor spawned",
      "type": "number"
    },
    "TargetLocation": {
      "$ref": "#/definitions/Vector",
      "description": "Where this cost actor should travel to"
    }
  },
  "required": [
    "ItemClass",
    "SpawnDelay",
    "TargetLocation"
  ]
}