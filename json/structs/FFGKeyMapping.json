{
  "type": "object",
  "properties": {
    "ActionKeyMapping": {
      "description": "An unknown type (likely an Unreal Engine or plugin type)."
    },
    "AxisKeyMapping": {
      "description": "An unknown type (likely an Unreal Engine or plugin type)."
    },
    "IsAxisMapping": {
      "type": "boolean"
    }
  },
  "required": [
    "ActionKeyMapping",
    "AxisKeyMapping",
    "IsAxisMapping"
  ]
}