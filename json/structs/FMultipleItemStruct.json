{
  "description": "Holds info about a schematic cost.",
  "type": "object",
  "properties": {
    "ItemCost": {
      "description": "Cost of schematic if there are more than once item in this array the true cost will be randomly selected.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/FItemAmount"
      }
    }
  },
  "required": [
    "ItemCost"
  ]
}