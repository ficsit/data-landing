{
  "type": "object",
  "properties": {
    "MinWaterDepth": {
      "description": "When at this minimum water depth, then use this footstep effect instead",
      "type": "number"
    },
    "Effect": {
      "$ref": "#/definitions/FFootstepEffect",
      "description": "The effect we want to play when hitting the surface"
    }
  },
  "required": [
    "Effect",
    "MinWaterDepth"
  ]
}