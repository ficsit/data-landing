{
  "type": "object",
  "properties": {
    "mImpactAudioMap": {
      "description": "Audio effects to play whenever a pawn hits the water surface with a specified velocity",
      "type": "array",
      "items": {
        "$ref": "#/definitions/FPawnImpactAudio"
      }
    }
  },
  "required": [
    "mImpactAudioMap"
  ]
}