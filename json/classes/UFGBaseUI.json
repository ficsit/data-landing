{
  "type": "object",
  "properties": {
    "mActivePopup": {
      "$ref": "#/definitions/UFGPopupWidget",
      "description": "Current active popup"
    }
  },
  "required": [
    "mActivePopup"
  ]
}