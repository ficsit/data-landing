{
  "type": "object",
  "properties": {
    "mDistance": {
      "description": "Total distance for point generation"
    },
    "mAngle": {
      "description": "Angle to generate points for"
    },
    "mStepSize": {
      "description": "An unknown type (likely an Unreal Engine or plugin type)."
    },
    "mGenerateMirroredPoints": {
      "description": "Should we also generate points for the negative angle value?"
    },
    "mCenterActor": {
      "description": "The actor (or actors) that will generate a cone in their facing direction",
      "type": "object",
      "properties": {
        "package": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "package"
      ]
    }
  },
  "required": [
    "mAngle",
    "mCenterActor",
    "mDistance",
    "mGenerateMirroredPoints",
    "mStepSize"
  ]
}