{
  "type": "object",
  "properties": {
    "mGuideLineType": {
      "$ref": "#/definitions/EGuideLineType",
      "description": "Defines what type of build guide line this is."
    }
  },
  "required": [
    "mGuideLineType"
  ]
}