{
  "type": "object",
  "properties": {
    "mInitializingFromInterface": {
      "description": "Initial Material Interface used to Init this manager"
    },
    "mMaterialInstances": {
      "description": "All materials are the same but their params are set to match the colors specified by the color gun",
      "type": "array",
      "items": {}
    },
    "mInstanceNames": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "mInitializingFromInterface",
    "mInstanceNames",
    "mMaterialInstances"
  ]
}