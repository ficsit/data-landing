{
  "description": "todo: (a bigish one) this is just a start of what should be a much more complete nativization of the spore flower",
  "type": "object",
  "properties": {
    "mTriggerActorClasses": {
      "description": "These actor types will trigger the spore flower to emit",
      "type": "array",
      "items": {
        "$ref": "#/definitions/classReference<AActor>"
      }
    },
    "mTriggerActors": {
      "description": "Holds a list of all actors inside trigger spehere",
      "type": "array",
      "items": {
        "$ref": "#/definitions/AActor"
      }
    }
  },
  "required": [
    "mTriggerActorClasses",
    "mTriggerActors"
  ]
}