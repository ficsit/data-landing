{
  "type": "object",
  "properties": {
    "mMapRedirectors": {
      "description": "Redirects for the maps when someone renames a map",
      "type": "array",
      "items": {
        "$ref": "#/definitions/FMapRedirector"
      }
    }
  },
  "required": [
    "mMapRedirectors"
  ]
}