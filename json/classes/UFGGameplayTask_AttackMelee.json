{
  "type": "object",
  "properties": {
    "mOnAttackFinished": {
      "description": "An unknown type (likely an Unreal Engine or plugin type)."
    },
    "mOnAttackFailed": {
      "description": "An unknown type (likely an Unreal Engine or plugin type)."
    },
    "mAttackTarget": {
      "$ref": "#/definitions/IFGAggroTargetInterface",
      "description": "The target we are attacking"
    },
    "mAttackClass": {
      "description": "Reference to a class.",
      "type": "object",
      "properties": {
        "package": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "package"
      ]
    },
    "mCachedPawn": {
      "$ref": "#/definitions/AFGEnemy",
      "description": "Cache the pawn that we have"
    }
  },
  "required": [
    "mAttackClass",
    "mAttackTarget",
    "mCachedPawn",
    "mOnAttackFailed",
    "mOnAttackFinished"
  ]
}