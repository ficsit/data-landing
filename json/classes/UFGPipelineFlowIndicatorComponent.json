{
  "type": "object",
  "properties": {
    "mPipeline": {
      "$ref": "#/definitions/AFGBuildablePipeline",
      "description": "Pipeline we indicate status for."
    }
  },
  "required": [
    "mPipeline"
  ]
}