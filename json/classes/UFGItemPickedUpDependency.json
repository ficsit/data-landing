{
  "type": "object",
  "properties": {
    "mItems": {
      "description": "The items that should have been picked up for this dependency to be met",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "package": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "package"
        ]
      }
    },
    "mRequireAllItemsToBePickedUp": {
      "description": "Do we need to have picked up all of the items to or is it enough with just one of them",
      "type": "boolean"
    }
  },
  "required": [
    "mItems",
    "mRequireAllItemsToBePickedUp"
  ]
}