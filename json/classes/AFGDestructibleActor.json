{
  "type": "object",
  "properties": {
    "mStaticMeshProxy": {
      "description": "The mesh that should be rendered before we start going destructible. Used for optimization as it has less triangles"
    },
    "mHasBeenFractured": {
      "description": "So we can save if this has been fractured",
      "type": "boolean"
    }
  },
  "required": [
    "mHasBeenFractured",
    "mStaticMeshProxy"
  ]
}