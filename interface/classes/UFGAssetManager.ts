import { Unknown } from '../native/unknown';

/**
 * Help us discard packages that shouldn't be cooked and add assets that's should always be cooked
 */
export interface UFGAssetManager extends Unknown<'UAssetManager'> {}
