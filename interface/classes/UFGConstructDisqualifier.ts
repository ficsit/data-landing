import { localized } from '../native/primitive';

export interface UFGConstructDisqualifier {
  /**
   * Text displayed to the player when they couldn't build
   */
  mDisqfualifyingText: localized;
}
