export interface UFGUseState {
  /**
   * If true, we will allocate a instance of the state, so we can store information in the state
   */
  mWantAdditonalData: boolean;

  /**
   * If true, we will allocate a instance of the state, so we can store information in the state
   */
  mIsUsableState: boolean;
}
