import { Unknown } from '../native/unknown';

export interface UFGCheatManager {
  /**
   * This is used to make picking the same classes in the cheat board easier
   */
  mPopularUClassChoices: Unknown<'UClass'>[];
}
