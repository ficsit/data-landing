import { Unknown } from '../native/unknown';

/**
 * Description of a radioactive source.
 */
export interface FRadioactiveSource {
  /**
   * Where the emitters are attached.
   */
  AttachRoot: Unknown<'USceneComponent'>;
}
